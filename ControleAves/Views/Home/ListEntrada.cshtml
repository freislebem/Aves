@model IEnumerable<ControleAves.Models.Entrada>

@{
    ViewData["Title"] = "ListEntrada";
}

<h2>ListEntrada</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Entrada_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entrada_tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entrada_idAve)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Entrada_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entrada_tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entrada_idAve)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
